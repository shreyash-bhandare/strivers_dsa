#include<bits/stdc++.h>
using namespace std;

void explainUSet(){
    unordered_set<int> uset;
    // lower and upper bound doesn't work, otherwise same functions.
    // no order, better complexity.
    // searching in O(1)
    // better at searching than other sets except when collision happens. 
    // in worst case O(N) time complexity but quite rarely happens.
}

int main(){
    explainUSet();
    return 0;
}